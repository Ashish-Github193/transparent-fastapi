global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: transparent-fastapi-demo
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:8000
        labels:
          service: example-app
