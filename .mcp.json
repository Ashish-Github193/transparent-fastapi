{
  "mcpServers": {
    "grafana": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "compose",
        "-f",
        "deploy/local/docker-compose.yml",
        "run",
        "--rm",
        "-T",
        "grafana-mcp"
      ],
      "env": {}
    }
  }
}